[package]
name = "dolpads"
version = "0.0.1"

[dependencies]
# Pin to the Sui framework commit that matches your validator or CLI.
# Update `rev` if your toolchain uses a different framework revision.
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "testnet" }

[addresses]
dolpads = "0x0"
sui = "0x2"

